# java接口：

public interface 接口名 {
    // 1. 常量（可选，默认 public static final）
    int MAX_SPEED = 120;

    // 2. 抽象方法（默认 public abstract，可省）
    void start();

}

### 使用规则：

- 接口不能new

- 类用 `implements` 实现，必须重写所有抽象方法（在类里面重写）；

- 一个类可以实现多个接口 

- `class`换成 `interface`就是接口的格式
