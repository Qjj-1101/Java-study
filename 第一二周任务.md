# 第 1-2 周学习计划：Java 语法 + 编程基础 + Git 入门

&gt; 时间：week 1-2  
&gt; 网络：5G 91（12:19）

---

## 1. Java 基础语法

- 数据类型与变量  
- 运算符、表达式、流程控制（if / switch / for / while）  
- 数组、字符串常用 API  
- 方法定义与调用、参数传递、返回值  

---

## 2. 面向对象入门

| 主题   | 要点                                 | 代码示例                                               |
| ---- | ---------------------------------- | -------------------------------------------------- |
| 类与对象 | 定义模板 vs 实例                         | `class Person { ... }`                             |
| 构造函数 | 无参、有参、重载、`this` 关键字                | `public Person(String name) { this.name = name; }` |
| 封装   | `private` 字段 + `getter` / `setter` | 生成 `getName()` / `setName()`                       |
| 继承   | `extends`、方法重写、`super`             | `class Student extends Person`                     |
| 多态   | 父类引用指向子类对象                         | `Person p = new Student();`                        |

---

## 3. 工具链

1. **JDK**  
   - 安装 JDK 11+（Oracle 或 OpenJDK）  
   - 配置 `JAVA_HOME` 与 `Path`  
2. **IntelliJ IDEA**  
   - 安装旗舰版/社区版  
   - 新建 Empty Project → Module → 新建 Java Class → 运行 `main`  
3. **Git / GitHub**  
   - 注册 GitHub 账号  
   - 安装 Git for Windows（或 macOS `brew install git`）  
   - 配置全局用户名、邮箱  
   - 基础命令：  
     
     ```bash
     git clone <url>
     git add .
     git commit -m "message"
     git push origin main
     ```
   - 创建 **个人仓库** `Java-study` → **每天 push 一次**，养成习惯

---

## 4. 算法 / 数据结构入门

- 刷题平台：**CodeTop**（https://codetop.cc）  
- 节奏：**一天一道**（先 Easy，再 Medium）  
- 记录：每题写 `README.md` → 题目链接 + 思路 + 代码 → 随代码一起 push  
- 参考书籍：《Effective Java（中文版）》黄色封面  
  - 4 个月内至少 **阅读 3 遍**  
  - 每遇到知识点（类、接口、异常、并发等）立即翻书对照规范  
  - 把书中建议落实到代码（例如 `equals` / `hashCode` / 枚举单例）

---

## 5. 每日 checklist（贴在桌面）

| 时间    | 任务                         | 状态  |
| ----- | -------------------------- | --- |
| 08:30 | 复习前一天 Git 记录 & 代码          | ☐   |
| 09:00 | 刷 CodeTop 1 题（先自己想 20 min） | ☐   |
| 10:30 | 对照题解 & 优化代码 → push         | ☐   |
| 14:00 | 阅读《Effective Java》1-2 条条款  | ☐   |
| 15:00 | 写 Java 练习（类/继承/封装）并 push   | ☐   |
| 22:30 | 总结今日收获 → 更新 README → push  | ☐   |

---

&gt; 坚持 14 天，形成“刷题-记录-push”肌肉记忆，后续学习将事半功倍！
